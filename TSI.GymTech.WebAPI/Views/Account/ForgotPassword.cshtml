@using TSI.GymTech.WebAPI.Models
@model TSI.GymTech.WebAPI.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Esqueceu sua senha?";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<div class="text-center">
    <h2 class="h4 text-gray-900 mb-4">@ViewBag.Title</h2>
</div>

<section id="loginForm" class="user">
    @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="form-login-group  input-group">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="fas fa-envelope"></i></span>
            </div>
            @Html.TextBoxFor(m => m.Email, new { placeholder = "Digite sue Email", @class = "form-control form-control-user" })
        </div>
        <div class="form-login-group ">
            <input type="submit" value="Email Link" class="btn btn-dark btn-user btn-block" />
        </div>
        <div class="text-center">
            @Html.ActionLink("Voltar para Login", "Login", null, new { @class = "small" })
        </div>
    }
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
